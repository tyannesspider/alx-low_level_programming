#include <stdio.h>

/**
 * mian -prints the usm of even fibonacci numbers
 * less than 4000000
 * Return: Nothing
 */

int main(void)

{
	int i =0;
	long j = 1, k = 2, sum =k;

	while (k + j < 4000000)
	{
	k += l;
	if (k % 2 ==0)
	sum += k;
	++i;
	}
	printf("%ld\n", sum);
	return (0);
}
